apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-cla-operator-app.fullname" . }}-test-connection"
  labels:
    {{- include "laa-cla-operator-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-cla-operator-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
